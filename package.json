{
  "name": "video-export-test",
  "version": "1.0.0",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "mkdir -p dist && cp index.html dist/index.html && cp style.css dist/style.css"
  },
  "dependencies": {
    "express": "^5.1.0",
    "multer": "^2.0.2",
    "ffmpeg-static": "5.3.0",
    "cors": "^2.8.5"
  }
}
